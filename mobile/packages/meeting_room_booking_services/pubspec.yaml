name: meeting_room_booking_services

description: meeting room booking external services

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage: ^8.1.0

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_lints: ^2.0.3
  dart_code_metrics_presets: ^2.4.0
  glob:
  path:

